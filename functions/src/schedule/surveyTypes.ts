import { SurveyType } from "../types";

/** All survey types in MVP. Order is stable for any iteration that needs it. */
export const ALL_SURVEY_TYPES: SurveyType[] = [
  "importance_check_in",
  "8dim",
  "state_of_change",
  "satisfaction_check_in"
];
